# Copy to .env.local for local development (do not commit .env.local)
# Set this to your Cloudflare Workers URL after deploying the API
VITE_API_URL=https://dessertkart-api.<your-account>.workers.dev
